import 'package:flutter/material.dart';

class WidgetsFile extends StatelessWidget {
  const WidgetsFile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}